const model = require("../schemas/ledger.schema");
const ApplicationLevelError = require("../MIDDLEWARES/ApplicationError.middleware");

class LedgerModel {
    createTransaction = () => {

    }
}

module.exports = LedgerModel;